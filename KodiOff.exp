#!/usr/bin/expect -f
set Host [lindex $argv 0]
set Orden [lindex $argv 1]
spawn ssh root@$Host $Orden
match_max 100000
expect {
        "yes/no" {
        send -- "yes\r"
    expect "*assword"
        send -- "libreelec\r" }
        "*assword" { send -- "libreelec\r" }
}

expect eof
